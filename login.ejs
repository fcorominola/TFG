<!DOCTYPE html>
<html lang="ca">
<head>
  <meta charset="UTF-8">
  <title>Inicia sessió</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      background-color: #73edff !important;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: Arial, sans-serif;
      position: relative;
      color: #000078;
    }
    form {
      background: rgba(255, 255, 255, 0.7);
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0,0,0,0.2);
      display: flex;
      flex-direction: column;
      align-items: center;
      min-width: 300px;
      color: #000078;
    }
    label, input {
      margin-bottom: 10px;
      width: 100%;
    }
    button {
      margin-top: 10px;
      background-color: #000078;
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 5px;
      cursor: pointer;
    }
    a {
      margin-top: 10px;
      color: #000078;
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <img src="/logo.png" alt="Logo" class="logo" style="position:absolute; top:20px; right:20px; width:100px;">
  <form method="POST" action="/login">
    <h2>Inicia sessió</h2>
    <label for="nom">Nom d'usuari:</label>
    <input type="text" name="nom" id="nom" required>

    <label for="password">Contrasenya:</label>
    <input type="password" name="password" id="password" required>

    <button type="submit">Inicia sessió</button>

    <a href="/register">No tens compte? Registra't</a>
  </form>
</bod
